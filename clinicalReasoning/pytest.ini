[pytest]
markers =
    unit: mark test as an offline unit test.
    integration: mark test as a controlled multi-component integration test.
    live: mark test as requiring live LLM API credentials (GEMINI_API_KEY or GROQ_API_KEY).
